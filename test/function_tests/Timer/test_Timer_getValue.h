#include <stdbool.h>
#include "Uart.h"
#include "Timer.h"

bool test_Timer_getValue(Timer_Id idTimer,
                         Timer* timer,
                         Uart_Id idUart,
                         Uart* uart);
